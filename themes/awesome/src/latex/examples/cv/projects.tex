[~ if (r.projects && r.projects.length) { ~]
%-------------------------------------------------------------------------------
%	SECTION TITLE
%-------------------------------------------------------------------------------
\cvsection{Projects}


%-------------------------------------------------------------------------------
%	CONTENT
%-------------------------------------------------------------------------------
\begin{cventries}
[~ _.each( r.projects, function(project) { ~]
%---------------------------------------------------------
  \cventry
    {[[ project.role ]]} % Role
    {[[ project.title ]]} % Title
    {[[ project.location ]]} % Location
    {[[ h.dateRange( project ) ]]} % Date(s)
    {[~ if (project.description && project.description.length ) { ~]
      \begin{cvitems} % Description
      \item{[[ project.description ]]}
      \end{cvitems}
    [~ } ~]}

[~ }); ~]
%---------------------------------------------------------
\end{cventries}
[~ } ~]
